import styled from 'styled-components';
import {
  space,
  layout,
  position,
  flexbox,
  border,
  shadow,
  background,
} from 'styled-system';

export const StyledBox = styled.div`
  ${space}
  ${layout}
  ${position}
  ${flexbox}
  ${border}
  ${shadow}
  ${background}
`;
